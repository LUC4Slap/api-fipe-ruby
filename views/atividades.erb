<!DOCTYPE html>
<html>
<head>
  <title>ATIVIDADES</title>
</head>
<body>
<div class="p-5">
    <h1 class="d-flex justify-content-center text-light">ATIVIDADES</h1>
    <div class="table-primary table-responsive">
      <table class="table align-middle">
        <thead>
          <tr>
            <% headers.each do |header| %>
              <th><%= header %></th>
            <% end %>
          </tr>
        </thead>
        <tbody>
          <% data.each do |row| %>
            <tr>
              <% headers.each do |header| %>
                <td><%= row[header] %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
